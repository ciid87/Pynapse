# Pynapse Configuration File
# ==========================
# Edit these paths and parameters for your analysis

# Input/Output directories (use absolute paths)
source_dir=/home/bob/images
dest_dir=/path/bob/pynapse_out

# Channel assignments
pre_channel=4
post_channel=3
pre_marker_channel=2
post_marker_channel=1

# Pre-synaptic detection parameters
pre_min=658
pre_noise=350
pre_size_low=0.08
pre_size_high=2.5
pre_blur=true
pre_blur_radius=2
pre_bkd=0
pre_use_maxima=true

# Post-synaptic detection parameters
post_min=578
post_noise=350
post_size_low=0.08
post_size_high=2.5
post_blur=true
post_blur_radius=2
post_bkd=0
post_use_maxima=true

# Overlap detection
overlap_pixels=1
dilate_pixels=0

# Stack processing
slice_number=5

# Parallelization options.
# - 'auto' (default): Automatically determine parallelism based on first image's memory usage
# - Integer (e.g., 4): Run exactly that many Fiji instances in parallel
# - 1, '1', 'false', or '0': Sequential processing (one at a time)
parallel_fiji=auto

# Bug replication flags
replicate_threshold_bug=false
use_original_broken_global_calibration=false
